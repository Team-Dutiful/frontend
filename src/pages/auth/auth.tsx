import styled from "styled-components";

const Auth = () => {
	return <AuthContainer>Auth페이지</AuthContainer>;
};

export default Auth;

const AuthContainer = styled.div`
	display: flex;
	flex-direction: column;
	height: 100vh;
	width: 360px;
`;
