import styled from "styled-components";

const Settings = () => {
	return <SettingsContainer>설정 페이지 입니다</SettingsContainer>;
};

export default Settings;

const SettingsContainer = styled.div`
	background-color: red;
`;
